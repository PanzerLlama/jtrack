twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    exception_controller: null
    form_themes:
        - 'form/form_tailwind_layout.html.twig'
    globals:
        app_version: '%env(APP_VERSION)%'
        mercure_url: '%env(MERCURE_PUBLISH_URL)%'